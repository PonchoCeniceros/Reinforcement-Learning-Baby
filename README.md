# Reinforcement Learning Baby 👩‍💻 🤖
Algoritmos de aprendizaje supervisado.

### Requerimientos 📋

Dependencias requeridas para poner en marcha el repositorio

```
Python >= 3.7
pip
virtualenv
git
```

### Instalación 🔧

Se debe descargar el presente repositorio en su ordenador. Se recomienda emplear
```
git clone [project_url]
```

Desde la carpeta raíz del repositorio, siga los siguientes comandos para generar un entorno virtual e instalar en el las librerías requeridas:

```
cd into apps folder
virtualenv venv (as administrator or sudo)
cd venv\
source bin\activate (linux)
Scripts\activate (windows)
pip install -r requirements.txt
```
